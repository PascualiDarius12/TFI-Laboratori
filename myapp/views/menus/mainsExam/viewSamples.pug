div(class="position-absolute top-50 start-50 translate-middle d-flex invisible" id="invisible" style="z-index:1")
    div.border.rounded(id="contenedor-flotante")
div(class="mt-3 p-4 border shadow-sm bg-white d-flex flex-column w-100 h-100 rounded" id="viewSample") 
    p(class="letraChida h4") Manager Samples
    span(class="letraChida h6 text-secondary") Create your sample or search for an existing one to modify or delete.
    .row.border.my-4.rounded.bg-white.shadow-sm(id="contenido-sample")
      .d-flex.flex-row.bg-color.rounded-top.text-white.w-100.align-items-center
           p(class="letraChida h5 p-3") Samples:
           .position-relative.mx-1
                input(class="inputSearch rounded-pill h6 p-2" placeholder="Type" id="nameSampleSearch" name="nameSampleSearch" type="text")
                .d-flex.align-items-center.position-absolute.end-0.top-50.px-2
                    i(class="fa-solid fa-magnifying-glass fa-xs agrandar" style="color: rgba(255, 255, 255, 0.648);" id="btnSearchSample")
           .d-flex.justify-content-center.p-3.w-100.align-items-end.flex-column
                i(class="fa-solid fa-plus text-white agrandar h4" id="btnAddSample") 
      div(class="table-responsive mt-4" id="tabla-samples")
                    table(class="table  table-hover h6 mt-3 rounded text-center")
                        thead(class="")
                            tr(class="")
                                th(scope="col") ID
                                th(scope="col") Type
                                th(scope="col") Detail
                                th(scope="col") Update 
                                th(scope="col") Delete 
                        tbody
                            each sample in samples
                                    tr(class= sample.active ? '' : 'table-danger')
                                        th(scope="row")  #{sample.id}
                                        td #{sample.type}
                                        td #{sample.detail}
                                        td 
                                            div(class="w-100 text-center")
                                                a(href="" id=sample.id class="btnUpdateSample")
                                                    i(class="fa-solid fa-pen px-1 text-warning agrandar")
                                        td 
                                            div(class="w-100 text-center")
                                                a(href="" id=sample.id class="btnDeleteSample")
                                                    i(class="fa-solid fa-trash px-1 text-danger agrandar")